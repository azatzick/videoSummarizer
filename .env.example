DJANGO_SECRET_KEY=change-me
DEBUG=1
ALLOWED_HOSTS=127.0.0.1,localhost

# Database selection: set to 1 to use SQLite locally
USE_SQLITE=1

# Postgres defaults (used when USE_SQLITE is not set to 1)
POSTGRES_DB=ai_blog_app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# External APIs
ASSEMBLYAI_API_KEY=
OPENAI_API_KEY=
